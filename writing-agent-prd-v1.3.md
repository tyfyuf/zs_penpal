# 写作专精辅助 Agent（桌面端）产品需求文档

版本：V1.3

## 修订说明

V1.3 根据 V1.2 评审结论修订，主要变化：

- 统一写作文档、对话、项目、归档区、回收站的生命周期。
- 明确项目级对话、文档级对话、右键上下文对话三种形式。
- Git 改为每个项目一个仓库，并明确跟踪范围。
- 重新定义“不代写”为“不自动改动成果文件”，不限制 AI 在对话中给出的内容。
- 补充上下文滑块的交互、摘要刷新策略、保存行为、导出选项和核心验收标准。
- 根据最终检查补充文件存储形态、单实例模式、文件快照、文本编码、Git 依赖、Token 统计和聊天摘要生成时机。
- 补充异常退出恢复、Git 开关、外部文件创建项目、取消生成和流式 `usage` 规则。

---

## 0. 产品定义

### 0.1 产品目标

辅助创作者决策，不替代创作者完成写作成果。

### 0.2 核心边界

系统必须遵守以下规则：

1. AI 不得自动修改写作文档内容。
2. AI 不得自动生成、替换或导出写作文档成品文件。
3. AI 可以在对话中输出建议、诊断、优化文本、改写示例和后续走向，不限制这些内容的表达形式。
4. AI 输出只能显示在对话区。用户只能通过手动复制后自行粘贴到编辑器，系统不提供“一键应用”“自动写入”或“自动插入”功能。
5. 用户手动复制、粘贴 AI 文本属于用户行为，不受上述限制。

### 0.3 目标平台

桌面端，首批仅支持 Windows。

---

## 1. 全局原则

### 1.1 项目制管理

所有作品以“项目”为单位组织。项目数据存储在用户指定的本地工作目录中。

### 1.2 快捷键

应用运行时，以下基础编辑快捷键在编辑器内保持最高优先级：

- 复制
- 粘贴
- 剪切
- 撤销
- 保存

系统不得注册会覆盖这些快捷键的 AI 功能快捷键。

### 1.3 存储安全

API Key 不得明文存入项目目录，不得纳入 Git。API Key 应使用本地加密存储，界面中默认打码显示。

### 1.4 数据存储形态

所有项目主数据以文件形式存储在工作目录内，不使用数据库保存写作文档、聊天记录、摘要和资源。聊天记录与摘要使用 Git 可读的文本文件保存；API Key 等敏感配置仍单独加密存储。

### 1.5 单实例模式

应用采用单实例模式：

- 第二个实例启动时静默退出。
- 若第二个实例由文件关联启动，则将文件路径传给第一个实例。
- 第一个实例收到路径后，若文件为 `.txt`、`.md` 或 `.csv`，则打开该文件；若该文件不属于任何项目，则自动创建新项目，并将该文件作为该项目的资源文件打开。
- 若文件类型不受支持，则拒绝并提示用户。
- 第二个实例被拦截时，第一个实例窗口获得焦点并闪动任务栏图标。

### 1.6 异常退出与恢复

- 所有项目文件采用原子写入，避免进程中断时留下半写文件。
- 编辑器停止输入后按设置中的自动保存间隔进行防抖保存，默认建议 5 秒。
- 应用维护本地恢复标记，记录最后打开的项目、文档和未提交状态；正常关闭时清除。
- 下次启动检测到恢复标记时，提示用户“上次异常退出，是否恢复上次打开的内容？”。
- 恢复日志和应用缓存不属于项目主数据，不进入项目 Git。
- Git 仍只在正常关闭时提交；异常退出后已自动保存的文件在下次正常关闭时提交。

---

## 2. 工作目录、初始化与 Git

### 2.1 首次启动

用户必须指定一个本地文件夹作为工作目录。

### 2.2 工作目录迁移

用户可在设置中修改工作目录。迁移时必须：

1. 迁移项目数据，而不是只迁移索引。
2. 若版本管理开关开启，保留每个项目的 Git 历史。
3. 若版本管理开关开启，迁移前自动提交当前变更；开关关闭时跳过提交。
4. 校验新目录可写且不存在冲突。
5. 迁移失败时保持原目录可用，并提示用户原因。

### 2.3 Git 仓库粒度

每个项目独立维护一个 Git 仓库。工作目录根目录只保存项目索引和应用配置，不作为一个统一 Git 仓库。

### 2.4 Git 跟踪范围

每个项目仓库默认跟踪以下数据：

- 写作文档
- 项目元数据
- 聊天记录
- 摘要数据
- 资源文件

以下内容不得进入项目 Git 仓库：

- API Key
- 其他全局敏感配置

### 2.5 自动提交

仅当版本管理开关开启时，每次用户正常关闭软件，系统按项目检测变更。对存在变更的项目执行一次自动提交：

1. 先保存未落盘的编辑器内容。
2. 再执行 Git 提交。
3. 提交信息使用系统时间戳。
4. 若 Git 不可用或提交失败，必须向用户提示，不能静默失败。

版本管理开关关闭时不执行自动提交。

### 2.6 版本历史与回滚

版本管理开关开启时，设置页提供“打开版本历史”入口，按项目查看提交记录。

回滚范围：

- 回滚该项目的写作文档、聊天记录、摘要数据和资源文件。
- 不回滚 API Key 和全局应用配置。

回滚前若存在未提交变更，系统应提示用户先提交或暂存，避免静默覆盖。

回滚前，系统自动关闭已打开的相关标签页。回滚完成后，由用户自行重新打开文档。

### 2.7 Git 运行依赖

版本管理功能提供开关。用户打开开关时，系统检测 Git；未安装时提示用户需要安装 Git。用户同意后自动安装；不同意则保持开关关闭。

用户关闭开关时，保留已有项目 Git 仓库和文件，但停止自动提交、版本历史和回滚功能；重新打开开关后恢复这些功能。

---

## 3. 左侧栏：项目管理与资源

### 3.1 树形节点

每个项目下包含：

- 写作文档节点
- 聊天节点
- 归档区节点
- 资源节点

### 3.2 写作文档节点

- 文档在右侧以标签页打开。
- 删除写作文档时执行软删除，进入回收站。

### 3.3 聊天节点

聊天分为三种形式：

1. 项目级对话
   - 挂载在项目根目录下。
   - 不关联具体写作文档。
   - 用户可在项目下主动新建。

2. 文档级对话
   - 挂载在具体写作文档下方，缩进显示。
   - 用户可在文档下主动新建。
   - 不提供上下文范围滑块。

3. 文档级上下文对话
   - 由用户在写作文档中右键调用“诊断”“走向”或“优化”时创建。
   - 同样挂载在对应写作文档下方。
   - 是唯一带上下文范围滑块的对话形式。

左侧树是软分类，聊天标签页不强制与当前编辑标签同步。用户点击左侧任意聊天节点，可在右侧打开对应对话。

### 3.4 归档区节点

- 每个项目拥有独立归档区。
- 默认折叠。
- 用于存储用户单独删除的对话，以及文档被彻底删除后自动转入的孤儿对话。
- 归档区不跨项目存在。
- 用户可在归档区中恢复或彻底删除对话。

### 3.5 资源节点

- 仅允许文本类文件，例如 `.txt`、`.md`、`.csv`。
- 禁止导入 PDF 和图片。
- 支持从左侧资源节点上传，也支持在会话中上传。

---

## 4. 数据生命周期

### 4.1 写作文档生命周期

写作文档状态：

- 正常
- 回收站
- 已彻底删除

规则：

1. 删除文档时，文档进入回收站，其文档级对话保持关联，但不在普通导航中显示。
2. 从回收站恢复文档时，文档及其关联对话一并恢复原关联。
3. 从回收站彻底删除文档时：
   - 文档本体销毁。
   - 文档摘要一并删除。
   - 仍关联在该文档下的文档级对话自动进入归档区，作为“孤儿对话”保留。
   - 这些孤儿对话不因文档已删除而自动移除。
4. 项目级对话不随单个写作文档删除而改变。
5. 若某个对话此前已被用户单独归档，之后其关联文档被彻底删除，则该归档对话一并移除。

### 4.2 对话生命周期

对话状态：

- 正常
- 用户归档
- 孤儿归档
- 已彻底删除

规则：

1. 用户单独删除任一对话时，该对话进入归档区，并保留与原项目或原文档的关联。
2. 若用户单独归档的对话，其关联文档或项目后来被彻底删除，则该归档对话一并移除。
3. 从归档区恢复对话时：
   - 原关联文档仍存在，则恢复到原文档下。
   - 原关联文档不存在但项目仍存在，则恢复为该项目下的项目级对话。
4. 用户在归档区彻底删除对话时，该对话销毁。

### 4.3 项目生命周期

项目状态：

- 正常
- 回收站
- 已彻底删除

规则：

1. 删除项目时必须弹出二次确认，确认后项目进入回收站。
2. 项目进入回收站时，项目内的文档、对话、资源、归档区保持完整关联。
3. 从回收站恢复项目时，整个项目树恢复。
4. 从回收站彻底删除项目时：
   - 项目下所有项目级对话一并删除。
   - 项目下所有文档级对话一并删除。
   - 项目归档区一并删除。
   - 项目摘要数据一并删除。
   - 项目资源文件一并删除。

### 4.4 摘要生命周期

- 文档摘要属于写作文档；文档彻底删除时，文档摘要删除。
- 聊天摘要属于对话；对话进入归档区时，聊天摘要随对话归档。
- 从归档区恢复对话时，聊天摘要恢复。
- 从归档区彻底删除对话时，聊天摘要销毁。
- 项目彻底删除时，其文档摘要和聊天摘要一并删除。

### 4.5 恢复冲突

恢复文档或项目时，如果同名对象已存在，系统默认将恢复对象重命名为副本，并提示用户，不静默覆盖。

---

## 5. 右侧主工作区

### 5.1 标签页体系

- 右侧采用标签页切换。
- 不支持多窗口平铺。
- 同一文档可关闭或重新打开；关闭前检测未保存改动。

### 5.2 编辑器标签页

功能：

- 编辑写作文档正文。
- 字体大小调节。
- 行号或段落编号显示。
- 暗黑模式。
- 不提供复杂排版功能。

文档存储：

- 默认使用 `.md` 扩展名，按纯文本方式编辑，不渲染 Markdown。
- 统一以 UTF-8 读写。
- 打开已有文档时保留原换行符。
- 新建文档统一使用 LF。

保存行为：

- 设置页提供全局自动保存间隔。
- 每个编辑器标签页提供独立保存按钮。
- 存在未保存改动时，标签页显示较轻的脏状态标记。
- 关闭未保存标签页时，提示用户保存或丢弃。

### 5.3 导出

单文档导出：

- Markdown `.md`
- 纯文本

项目导出：

- 导出为 Zip 文件。
- 默认勾选写作文档和资源文件。
- 默认不勾选聊天记录、归档区、摘要数据和 Git 历史。
- 用户可在导出前调整是否包含：
  - 聊天记录
  - 资源文件
  - 归档区
  - 摘要数据
  - Git 历史

### 5.4 聊天标签页

- 显示 AI 对话界面。
- 项目级对话标签显示项目名。
- 文档级对话和上下文对话标签显示关联文档名。
- 聊天标签页不强制与当前编辑标签同步。

### 5.5 文件上传交互

聊天输入框旁提供“上传文件”按钮。用户可选择：

1. 从资源区选取
   - 弹出当前项目资源文件列表。
   - 用户勾选文件后，文件内容作为本次会话上下文。

2. 从本地上传
   - 打开系统文件选择器。
   - 仅允许文本类文件。
   - 所选文件自动复制到当前项目资源节点。
   - 文件内容作为本次会话上下文。

无论哪种上传方式：

- 文件仅对本次会话有效。
- AI 不会自动扫描或读取资源节点中的其他文件。
- 会话结束后，已上传内容不会自动成为其他会话的背景知识。
- 上传时保存文件内容快照。历史会话使用上传时的内容，后续修改或删除原文件不影响该会话。
- 资源快照写入该对话内部。

### 5.6 AI 内容复制

AI 输出支持：

- 全篇复制。
- 鼠标选中后复制。

---

## 6. 右键菜单与上下文控制

这是产品的核心功能入口。

### 6.1 右键菜单

编辑器空白处右键：

- 诊断
- 走向

选中文字后右键：

- 诊断
- 走向
- 优化

### 6.2 触发规则

点击任一 AI 选项后，系统不立即发送请求，而是：

1. 根据当前右键位置和规则自动选取初始上下文。
2. 创建一条新的文档级上下文对话。
3. 在对话窗口中显示上下文控制面板。

每次右键调用均创建一条新的文档级上下文对话，不复用已有对话。

### 6.3 默认上下文范围

诊断和优化：

- 以光标或选区为中心。
- 默认抓取前后各 200 字。
- 若有选区，选区内容属于核心上下文。

走向：

- 前文必须包含光标前内容，作为前情提要。
- 默认前文 500 字。
- 若光标后有内容，默认后文 200 字。
- 若光标后无内容，默认后文 0 字。
- 若有选区，以选区起点作为前文终点，以选区终点作为后文起点。

### 6.4 上下文控制面板

面板位于对话窗口中，包含：

- 前文、后文两个滑块。
- 前文、后文两个精确数字输入框。
- 编辑器中对应文本区域实时高亮。

边界行为：

- 光标或选区位于文档开头时，前文字数为 0，前文滑块禁用，只能调整后文。
- 光标或选区位于文档结尾时，后文字数为 0，后文滑块禁用，只能调整前文。
- 面板应跟随选区变化。

### 6.5 确认与输出

用户确认发送后：

1. 系统按设定范围抓取文本。
2. 按 OpenAI 兼容 API 调用 AI，采用流式输出。
3. 用户可取消正在生成的回答；取消后仅保留取消前的用户消息，不保留已生成的 AI 部分内容。
4. 调用失败时，在对话中显示错误信息。
5. AI 结果显示在当前上下文对话中，不写入编辑器。

### 6.6 输出后的上下文调整

AI 输出结束后，上下文滑块在对话窗口上方持续出现：

- AI 输出期间滑块不可调整。
- AI 输出结束后，用户可再次调整前文或后文范围。
- 如果关联文档未打开，系统提示并建议自动打开该文档。
- 用户调整范围后，系统提示：

> 检测到上下文范围调整，是否自动重新生成最近的 AI 回答？

用户确认后，基于新范围重新生成该轮回答。

重新生成时，替换最近一条 AI 回答，并保留用户输入。

### 6.7 字数与 Token

- 界面向用户展示“字数”。
- 系统内部按所选模型的 tokenizer 估算 token。
- 模型上下文上限由用户在设置中配置，默认 256k token。
- 系统维护内部上下文预算，避免超出该上限。

当上下文可能超出模型上限时，系统按以下优先级自动保留：

1. 用户当前选区或核心内容。
2. 选区附近的上下文。
3. 当前对话历史。
4. 其他文档摘要和对话摘要。

系统自动截断低优先级内容，此过程对用户透明。

### 6.8 空文档

如果当前文档为空，或读取不到任何本文档上下文：

1. AI 可使用当前项目内其他文档摘要和对话摘要进行回答。
2. 如果也没有其他摘要，AI 应询问用户想写什么，而不是直接编造正文。

---

## 7. 摘要系统

### 7.1 摘要类型

文档摘要：

- 核心冲突
- 角色动机
- 章节功能

聊天摘要：

- 为对话生成简短索引标签。
- 例如“关于第 3 章的修改建议”。

### 7.2 触发条件

系统不提供手动刷新按钮。

当用户在关联了写作文档的对话中发起请求时，系统静默检测该关联文档状态。

触发文档摘要更新的条件：

`增删改字符总量 / max(上次摘要时的快照长度, 1) > 30%`

或：

`文档净增加字数 > 500`

两者为 OR 关系。

### 7.3 更新行为

若需要更新：

1. 系统先显示“正在更新摘要”状态。
2. 生成新摘要后，将其附加到本次请求的 Prompt。
3. 更新完成后保存新的文档快照。

若不需要更新：

- 沿用旧摘要。

若摘要生成失败：

- 本次请求沿用旧摘要。
- 标记下次重试。
- 不阻塞用户当前请求。

### 7.4 摘要开关

用户可在设置中关闭整个摘要功能。关闭后，系统不再生成或附加文档摘要，也不生成或更新聊天摘要。

### 7.5 聊天摘要生成时机

当摘要功能开启，且对话中发生过至少一次用户与 AI 的交换时，系统在关闭该对话窗口后，在后台生成或更新聊天摘要。

- 只有出现新消息时才更新摘要。
- 摘要生成或更新不阻塞对话窗口关闭。
- 用户关闭软件时，若仍有摘要生成或更新任务，应用等待其完成后再退出；若超时未完成，则完成退出，并在下次启动时提示用户该错误，用户可选择重试。
- 生成失败时标记重试，后续关闭窗口或启动应用时重试。
- 对话未关闭前不强制生成摘要。

---

## 8. 设置界面

### 8.1 工作目录

- 显示当前路径。
- 提供修改入口，并触发迁移流程。

### 8.2 API 配置

- API Base URL。
- API Key，使用本地加密存储，界面默认打码。
- 模型名称。
- 模型上下文上限，默认 256k token。
- 联通测试按钮。

API 按 OpenAI 兼容协议接入，支持流式输出和取消生成。

当前版本只支持一个模型，不支持多模型切换。

### 8.3 自动保存

- 全局自动保存间隔，默认建议 5 秒。
- 该间隔对所有编辑器标签页生效。

### 8.4 摘要功能

- 提供自动摘要开关。

### 8.5 用量统计

- 近 30 天 Token 消耗趋势折线图。
- 当日每小时 Token 消耗趋势折线图。
- 显示今日总消耗和历史总消耗。

流式请求开启 `include_usage`，确保响应包含 `usage`。

Token 统计在每次 API 响应后读取 `usage`，累加 `prompt_tokens`、`completion_tokens` 和 `total_tokens`。

若响应中缺少 `usage`，该次调用标记为未计入，不计入图表。

### 8.6 版本管理

- 提供版本管理开关；打开时按第 2.7 条检测并安装 Git。
- 版本管理开关开启时，提供“打开版本历史”按钮。
- 按项目执行 Git 回滚。

---

## 9. 核心验收标准

以下场景用于验证 V1.3 的关键规则。

### 9.1 不自动写入

Given 用户在对话中获得 AI 优化文本  
When 用户只点击复制，未在编辑器中进行粘贴  
Then 编辑器内容保持不变

### 9.2 快捷键

Given 编辑器处于输入状态  
When 用户按下复制、粘贴、剪切、撤销或保存快捷键  
Then 系统执行对应的基础编辑操作，而不是 AI 功能

### 9.3 软删除文档

Given 一个写作文档拥有文档级对话  
When 用户删除该文档  
Then 文档进入回收站，其文档级对话保持关联但不显示在普通导航中

### 9.4 恢复文档

Given 一个写作文档及其文档级对话处于回收站  
When 用户恢复该文档  
Then 文档与对话恢复原关联

### 9.5 彻底删除文档

Given 一个写作文档从回收站被彻底删除  
When 删除完成  
Then 文档销毁，文档摘要删除，仍关联的文档级对话作为孤儿对话进入该项目归档区

### 9.6 项目彻底删除

Given 一个项目处于回收站  
When 用户彻底删除该项目  
Then 项目下所有项目级对话、文档级对话、归档区、摘要数据和资源文件一并删除

### 9.7 归档区独立性

Given 项目 A 和项目 B 各自拥有归档区  
When 用户彻底删除项目 A  
Then 项目 B 的归档区不受影响

### 9.8 Git 隔离

Given 项目 A 和项目 B 各自拥有独立 Git 仓库  
When 用户回滚项目 A  
Then 项目 B 的文件和版本历史不发生变化

### 9.9 资源权限

Given 资源节点中存在多个文本文件  
When 用户在某个会话中只勾选文件 X 上传  
Then AI 只读取文件 X，不自动读取其他资源文件

### 9.10 上下文边界

Given 光标位于文档开头  
When 打开上下文控制面板  
Then 前文为 0 字，前文滑块禁用，只能调整后文

### 9.11 输出后调整

Given 一条上下文对话已生成 AI 回答  
When 用户调整前文或后文滑块  
Then 系统提示是否重新生成最近的 AI 回答

### 9.12 摘要关闭

Given 用户在设置中关闭自动摘要  
When 用户发起新的关联文档请求  
Then 系统不生成或附加新摘要

### 9.13 未保存关闭

Given 编辑器存在未保存改动  
When 用户尝试关闭该标签页  
Then 系统提示保存或丢弃

### 9.14 工作目录迁移

Given 用户修改工作目录并确认迁移  
When 迁移成功  
Then 项目、文档、对话、摘要和 Git 历史均可在新目录访问

### 9.15 单实例文件传递

Given 应用已有第一个实例正在运行  
When 用户通过文件关联启动第二个实例  
Then 第二个实例静默退出，第一个实例获得焦点并打开该文件

### 9.16 资源快照

Given 用户已在会话中上传资源文件 X  
When 用户之后修改或删除资源节点中的 X  
Then 该历史会话仍使用上传时保存的 X 内容

### 9.17 新建文档换行符

Given 用户在应用内新建一个写作文档  
When 保存文档  
Then 文档使用 LF 换行符，并以 UTF-8 编码保存

### 9.18 回滚关闭标签页

Given 用户执行项目回滚  
When 回滚开始  
Then 已打开的相关标签页自动关闭，回滚完成后由用户重新打开

### 9.19 聊天摘要生成

Given 用户在一个对话中完成过至少一次 AI 交换  
When 用户关闭该对话窗口  
Then 系统在后台生成或更新聊天摘要，不阻塞窗口关闭

### 9.20 单个对话删除

Given 用户单独删除一个文档级对话  
When 对话进入归档区且其关联文档仍存在  
Then 用户可将其恢复到原文档下

### 9.21 用户归档对话随文档删除移除

Given 一个文档级对话已被用户单独归档，其关联文档仍存在  
When 该关联文档被彻底删除  
Then 该归档对话一并移除

### 9.22 孤儿对话恢复

Given 一个文档级对话因关联文档彻底删除而成为孤儿对话  
When 用户从归档区恢复该对话  
Then 该对话恢复为原项目下的项目级对话

### 9.23 异常退出恢复

Given 应用异常退出并留下恢复标记  
When 用户下次启动应用  
Then 系统提示是否恢复上次打开的内容

### 9.24 外部文件创建项目

Given 用户通过文件关联打开一个不属于任何项目的文本文件  
When 第一个实例收到该路径  
Then 系统自动创建新项目，并将该文件作为项目资源文件打开

### 9.25 Git 开关

Given 用户打开版本管理开关且系统未安装 Git  
When 用户同意安装  
Then 系统自动安装 Git；若用户拒绝，则开关保持关闭

### 9.26 取消生成

Given AI 正在生成回答  
When 用户取消生成  
Then 对话仅保留取消前的用户消息，不保留已生成的部分回答

### 9.27 摘要完成后退出

Given 用户关闭软件时仍有聊天摘要生成任务  
When 用户关闭软件  
Then 应用等待摘要生成或更新完成；若超时未完成，则退出并在下次启动时提示错误，用户可选择重试

### 9.28 流式 usage

Given 系统发起流式 API 请求  
When 请求配置  
Then 开启 `include_usage`，以便读取 `usage` 字段

### 9.29 不支持的外部文件

Given 用户通过文件关联打开一个非文本文件  
When 第一个实例收到该路径  
Then 系统拒绝打开并提示用户

### 9.30 关闭 Git 开关

Given 项目已有 Git 仓库  
When 用户关闭版本管理开关  
Then 系统保留仓库和文件，但停止自动提交、版本历史和回滚功能

---

## 10. 已确认规则

以下澄清已确认，并已纳入正文：

1. 每次在文档中右键调用“诊断”“走向”或“优化”，都会创建一条新的文档级上下文对话。
2. 用户在输出后调整滑块并确认重新生成时，系统替换最近一条 AI 回答，同时保留用户输入。
3. 项目导出时，写作文档和资源文件默认勾选，聊天、归档区、摘要和 Git 历史默认不勾选。
4. 项目主数据以文件形式存储，聊天记录和摘要使用 Git 可读的文本文件保存。
5. 应用采用单实例模式，文件关联启动时把路径交给第一个实例。
6. 文档彻底删除时，仍关联的文档级对话作为孤儿对话保留在归档区。
7. 文档摘要随文档删除；聊天摘要随对话归档、恢复或销毁。
8. API 按 OpenAI 兼容协议接入，支持流式输出和取消生成。
9. 模型上下文上限由用户配置，默认 256k token。
10. 上传到会话的资源文件使用上传时快照。
11. 写作文档默认 `.md`、纯文本编辑、UTF-8，新建文档使用 LF。
12. 版本管理开关打开时检测 Git；用户同意后自动安装，拒绝则保持关闭。
13. 回滚前自动关闭相关标签页。
14. 每次 API 响应后读取 `usage` 累加 Token。
15. 关闭对话窗口后生成或更新聊天摘要。
16. 异常退出采用原子写入、恢复标记和启动恢复提示。
17. 关闭软件时等待后台摘要任务完成；超时则退出，并在下次启动时提示错误，用户可选择重试。
18. 外部文件不属于项目时自动创建项目并作为资源文件打开。
19. Git 开关打开时检测并自动安装 Git；用户拒绝则保持关闭。
20. 空文档使用当前项目内其他摘要。
21. 取消生成仅保留取消前的用户消息。
22. 流式请求开启 `include_usage`。
23. 资源快照写入对话内部。
24. 版本管理开关关闭时不初始化、不提交、不显示版本历史；关闭后保留已有仓库但停止相关功能。
25. 摘要功能关闭时，文档摘要和聊天摘要均不生成或更新。
26. 文件关联打开时，非 `.txt`、`.md`、`.csv` 文件拒绝并提示用户。
